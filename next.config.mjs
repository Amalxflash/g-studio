/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com', 'media.istockphoto.com'], // Add both domains
    },
  };
  
  export default nextConfig;
  